

inspect=>@"creates the default roles 
in the system"


magix.data.count
  prototype
    type=>magix.core.role
if=>[magix.data.count][count].Value==0
  magix.data.save
    object
      type=>magix.core.role
      name=>admin

