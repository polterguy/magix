

inspect=>@"test file for verifying parameters are passed correctly
into and out from hyper lisp files.&nbsp;&nbsp;
used in combination with [magix.test.execute-file-with-parameters]"


if=>not-equals
  lhs=>[$][mumbo].Value
  rhs=>howdy
  code
    throw=>input parameter not accessible in file
set=>[$][jumbo].Value
  value=>5
